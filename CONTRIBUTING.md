# 贡献指南

感谢你对 `yew-tab` 感兴趣！本文档提供了项目结构、开发流程和贡献规范的详细说明。

## 🛠️ 技术栈

本项目基于以下核心技术构建：

- **Extension Platform**: WebExtension (Manifest V3)
- **Frontend Framework**: React 18
- **Language**: TypeScript 5.5+
- **Build Tool**: Vite 5.4+
- **Linter**: ESLint (w/ typescript-eslint)
- **Package Manager**: pnpm

## 📂 项目结构

```
yew-tab/
├── src/
│   ├── background/     # Service Worker 后台脚本
│   ├── newtab/         # 新标签页前端应用 (React root)
│   │   ├── components/ # UI 组件
│   │   ├── main.tsx    # 入口文件
│   │   └── app.tsx     # 根组件
│   ├── lib/            # 核心业务逻辑与 API 封装
│   │   ├── bookmarks/  # Chrome Bookmarks API 封装
│   │   ├── messaging/  # 内部消息通信
│   │   └── storage/    # 数据持久化
│   └── shared/         # 前后端共享资源
│       ├── types/      # TypeScript 类型定义
│       └── constants/  # 全局常量
├── helloagents/        # 项目文档与规划 (Wiki, Architecture, Plans)
├── public/             # 静态资源 (manifest.json 等)
├── dist/               # 构建产物 (git ignored)
└── tests/              # 测试文件
```

## 🚀 开发指南

### 1. 环境准备
确保你的系统安装了：
- **Node.js**: 推荐 LTS 版本。
- **pnpm**: 本项目使用 `pnpm` 进行包管理。

### 2. 安装依赖
```bash
pnpm install
```

### 3. 常用命令
- **启动开发服务器**: `pnpm dev`
  - 这将启动 Vite 的监听模式。
- **构建生产版本**: `pnpm build`
  - 输出目录为 `dist/`。这是加载到浏览器中的最终代码。
- **代码检查**: `pnpm lint`

### 4. 在浏览器中调试
1. 运行 `pnpm build` 生成 `dist` 目录。
2. 打开 Chrome/Edge 的扩展管理页面：
   - Edge: `edge://extensions`
   - Chrome: `chrome://extensions`
3. 开启右上角的 **"开发人员模式" (Developer mode)**。
4. 点击 **"加载解压缩的扩展" (Load unpacked)**。
5. 选择项目根目录下的 `dist` 文件夹。
6. 修改代码后，重新运行 `pnpm build`，并在扩展管理页面点击 **刷新** 按钮即可看到更改。

## 📏 规范与约定

### 代码风格
- 项目配置了 ESLint，请确保在提交前运行 `pnpm lint` 无报错。
- 推荐使用 VS Code 并安装 ESLint 插件以实现自动修复。
- **文件命名**：统一使用 `kebab-case` (小写短横线)，例如 `bookmark-card.tsx`。

### 提交规范 (Conventional Commits)
请遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范编写 Commit Message。

**格式**：`<type>(<scope>): <subject>`

**常用类型**：
- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档变更
- `style`: 代码格式调整（不影响逻辑）
- `refactor`: 代码重构
- `chore`: 构建过程或辅助工具的变动

**示例**：
- `feat(bookmarks): add delete folder functionality`
- `fix(ui): fix search bar alignment`

### 语言
- 代码注释、文档、Commit Message 推荐使用 **中文**，以保持与现有文档的一致性。
